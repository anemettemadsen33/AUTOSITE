name: Self-hosted Runner Test 🖥️

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, windows]
    steps:
      - name: ✅ Runner funcționează!
        run: echo "Salut de pe PC-8406! Workflow-ul rulează cu succes pe self-hosted runner 🟢"
