name: AI Reviewer PR Comments

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai-review:
    name: GPT AI Review
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Leave GPT Review Comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: ai-review
          message: |
            🤖 This PR has been submitted for automated AI review.
            A review will be generated shortly.

      - name: Trigger AI Review via external API (placeholder)
        run: |
          echo "🔧 Replace this with a real AI review call (ex: OpenAI + custom script)"
